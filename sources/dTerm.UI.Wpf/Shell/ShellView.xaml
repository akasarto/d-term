<Window
	x:Class="dTerm.UI.Wpf.Shell.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:consoles="clr-namespace:dTerm.UI.Wpf.Consoles"
    FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
	TextElement.Foreground="{DynamicResource MaterialDesignBody}"
	Background="{DynamicResource MaterialDesignPaper}"
	WindowStartupLocation="CenterScreen"
	TextElement.FontWeight="Medium"
	TextElement.FontSize="14"
	WindowState="Maximized"
	d:DesignWidth="1280"
	d:DesignHeight="800"
	mc:Ignorable="d"
	Title="dTerm"
	MinWidth="1280"
	MinHeight="800"
	x:Name="shellView"
>

	<Grid>

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<materialDesign:ColorZone Mode="Inverted" Padding="16" Grid.Row="0">
			<DockPanel LastChildFill="False">
				<StackPanel Orientation="Horizontal">
					<ToggleButton Style="{DynamicResource MaterialDesignHamburgerToggleButton}" />
					<TextBlock VerticalAlignment="Center" Margin="16 0 0 0">Material Design In XAML Toolkit</TextBlock>
				</StackPanel>
				<Button DockPanel.Dock="Right" Cursor="Hand" Command="{Binding CreateConsole}" VerticalAlignment="Center">
					<Button.Style>
						<Style TargetType="Button">
							<Setter Property="Template">
								<Setter.Value>
									<ControlTemplate>
										<Border Background="Transparent">
											<materialDesign:PackIcon Kind="Settings" Margin="4 0 4 0" Width="24" Height="24" Foreground="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=materialDesign:ColorZone}, Path=Foreground}" />
										</Border>
									</ControlTemplate>
								</Setter.Value>
							</Setter>
						</Style>
					</Button.Style>
				</Button>
			</DockPanel>
		</materialDesign:ColorZone>

		<StackPanel Grid.Row="1">
			<DockPanel Margin="15" LastChildFill="False">
				<StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
					<materialDesign:PackIcon Kind="Console" Width="24" Height="24" VerticalAlignment="Center" />
					<TextBlock Margin="8 0 0 0" VerticalAlignment="Center">Available Consoles</TextBlock>
				</StackPanel>
				<StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
					<materialDesign:Chip Margin="15 0 0 0" Icon="C">Cmd</materialDesign:Chip>
					<materialDesign:Chip Margin="15 0 0 0" Icon="G">Git Bash</materialDesign:Chip>
					<materialDesign:Chip Margin="15 0 0 0" Icon="P">Power Shell</materialDesign:Chip>
					<materialDesign:Chip Margin="15 0 0 0" Icon="U">Ubuntu Bash</materialDesign:Chip>
					<Border BorderBrush="{DynamicResource MaterialDesignDivider}" Margin="15 0 15 0" BorderThickness="1 0 0 0" />
					<materialDesign:PopupBox Style="{StaticResource MaterialDesignMultiFloatingActionPopupBox}" PlacementMode="BottomAndAlignCentres">
						<StackPanel>
							<Button ToolTip="GitHub" Command="{Binding CreateConsole}">
								<materialDesign:PackIcon Kind="GithubCircle" Height="20" Width="20" />
							</Button>
							<Button ToolTip="Twitter" Background="{DynamicResource PrimaryHueMidBrush}" Foreground="{DynamicResource PrimaryHueMidForegroundBrush}">
								<materialDesign:PackIcon Kind="Twitter" />
							</Button>
							<Button ToolTip="Chat">
								<materialDesign:PackIcon Kind="Message" />
							</Button>
							<Button ToolTip="Email" Background="{DynamicResource SecondaryAccentBrush}" Foreground="{DynamicResource SecondaryAccentForegroundBrush}">
								<materialDesign:PackIcon Kind="Email" />
							</Button>
							<Button ToolTip="Feel like you want to make a donation?">
								<materialDesign:PackIcon Kind="Gift" />
							</Button>
						</StackPanel>
					</materialDesign:PopupBox>
				</StackPanel>
			</DockPanel>
			<Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0 0 0 1" />
		</StackPanel>

		<Path Style="{StaticResource logoVisualStyle}" Grid.Row="2" />

		<ContentControl Content="{Binding Tmp}" Grid.Row="2" />

	</Grid>

</Window>
