<Window
	x:Class="dTerm.UI.Wpf.Shell.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:consoles="clr-namespace:dTerm.UI.Wpf.Consoles"
	xmlns:dockablz="clr-namespace:Dragablz.Dockablz;assembly=Dragablz"
	xmlns:dragablz="clr-namespace:Dragablz;assembly=Dragablz"
    FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
	TextElement.Foreground="{DynamicResource MaterialDesignBody}"
	Background="{DynamicResource MaterialDesignPaper}"
	WindowStartupLocation="CenterScreen"
	TextElement.FontWeight="Medium"
	TextElement.FontSize="14"
	WindowState="Maximized"
	d:DesignWidth="1280"
	d:DesignHeight="800"
	mc:Ignorable="d"
	Title="dTerm"
	MinWidth="1280"
	MinHeight="800"
	x:Name="shellView"
>

	<Grid>

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<materialDesign:ColorZone Mode="Inverted" Padding="16" Grid.Row="0">
			<DockPanel LastChildFill="False">
				<StackPanel Orientation="Horizontal">
					<ToggleButton Style="{DynamicResource MaterialDesignHamburgerToggleButton}" />
					<TextBlock VerticalAlignment="Center" Margin="16 0 0 0">Material Design In XAML Toolkit</TextBlock>
				</StackPanel>
				<ListBox Style="{StaticResource MaterialDesignToolToggleListBox}" SelectedIndex="0" DockPanel.Dock="Right" VerticalAlignment="Center">
					<ListBoxItem>
						<materialDesign:PackIcon Kind="GridLarge" />
					</ListBoxItem>
					<ListBoxItem>
						<materialDesign:PackIcon Kind="ReorderVertical" />
					</ListBoxItem>
					<ListBoxItem>
						<materialDesign:PackIcon Kind="ReorderHorizontal" />
					</ListBoxItem>
				</ListBox>
			</DockPanel>
		</materialDesign:ColorZone>

		<StackPanel Grid.Row="1">
			<DockPanel Margin="15" LastChildFill="False">
				<StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
					<materialDesign:PackIcon Kind="Console" Width="24" Height="24" VerticalAlignment="Center" />
					<TextBlock Margin="8 0 0 0" VerticalAlignment="Center">Available Consoles</TextBlock>
				</StackPanel>
				<StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
					<Button Margin="15 0 0 0" Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" ToolTip="Cmd">
						<materialDesign:PackIcon Kind="Console" Height="24" Width="24" />
					</Button>
					<Button Margin="15 0 0 0" Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" ToolTip="Git Bash">
						<materialDesign:PackIcon Kind="Git" Height="24" Width="24" />
					</Button>
					<Button Margin="15 0 0 0" Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" ToolTip="Power Shell">
						<materialDesign:PackIcon Kind="Power" Height="24" Width="24" />
					</Button>
					<Button Margin="15 0 0 0" Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" ToolTip="Ubuntu Bash">
						<materialDesign:PackIcon Kind="Ubuntu" Height="24" Width="24" />
					</Button>
					<Border BorderBrush="{DynamicResource MaterialDesignDivider}" Margin="15 0 15 0" BorderThickness="1 0 0 0" />
					<Button Style="{StaticResource MaterialDesignFloatingActionDarkButton}" ToolTip="MaterialDesignFloatingActionButton">
						<materialDesign:PackIcon Kind="Settings" Height="24" Width="24" />
					</Button>
				</StackPanel>
			</DockPanel>
			<Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0 0 0 1" />
		</StackPanel>

		<Path Style="{StaticResource logoVisualStyle}" Grid.Row="2" />
		
		<dockablz:Layout x:Name="MdiLayout" FloatingItemHeaderMemberPath="Name" FloatingItemDisplayMemberPath="ProcessHost" FloatingItemsSource="{Binding ConsoleInstances}" Grid.Row="2">
			<!--
				<dockablz:Layout.FloatingItemTemplate>
					<DataTemplate>
						<Border Margin="15 0 15 15" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
							<consoles:ConsoleInstanceView DataContext="{Binding}" />
						</Border>
					</DataTemplate>
				</dockablz:Layout.FloatingItemTemplate>
				-->
		</dockablz:Layout>

	</Grid>

</Window>
