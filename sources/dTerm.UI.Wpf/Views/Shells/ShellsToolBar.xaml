<ToolBar
    x:Class="dTerm.UI.Wpf.Views.ShellsToolBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:l="clr-namespace:dTerm.Core.Localization;assembly=dTerm.Core"
    xmlns:local="clr-namespace:dTerm.UI.Wpf.Views"
    mc:Ignorable="d" 
    d:DesignHeight="450"
    d:DesignWidth="800"
    Style="{DynamicResource MaterialDesignToolBar}"
    Header="{x:Static l:Content.Shells}"
    ClipToBounds="False"
    Loaded="OnLoaded"
>

    <local:ShellsToolBarAddButton />

    <Separator />

    <Button x:Name="loader" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" IsEnabled="False">
        <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" IsIndeterminate="True" Width="24" Height="24" />
    </Button>

    <ItemsControl x:Name="list" Visibility="Collapsed" Margin="0" Padding="0">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <local:ShellsToolbarOptionButton ViewModel="{Binding}" />
            </DataTemplate>
        </ItemsControl.ItemTemplate>
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
    </ItemsControl>

</ToolBar>
