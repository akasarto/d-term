<Window
	x:Class="dTerm.UI.Wpf.Views.ConsoleView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
	xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
	xmlns:materialdesign="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:behaviors="clr-namespace:dTerm.UI.Wpf.Behaviors"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:local="clr-namespace:dTerm.UI.Wpf.Views"
	mc:Ignorable="d"
	Title="{Binding Instance.Name}"
	Height="300"
	Width="300"
	ResizeMode="CanResizeWithGrip"
	WindowStartupLocation="CenterOwner"
	ShowInTaskbar="False"
	ShowActivated="False"
>
	<!--<i:Interaction.Triggers>
		<i:EventTrigger EventName="Closing">
			<ei:CallMethodAction TargetObject="{Binding}" MethodName="OnViewClosing" />
		</i:EventTrigger>
	</i:Interaction.Triggers>-->

	<i:Interaction.Behaviors>
		<behaviors:ViewHandleProviderBehavior Handle="{Binding ConsoleViewHandle, Mode=TwoWay}" />
	</i:Interaction.Behaviors>

	<ContentControl Content="{Binding ConsoleHwndHost}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

</Window>
