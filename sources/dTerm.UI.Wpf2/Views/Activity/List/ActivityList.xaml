<local:ActivityListBase
    x:Class="dTerm.UI.Wpf.Views.ActivityList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:dTerm.UI.Wpf.Views"
    mc:Ignorable="d" 
    d:DesignHeight="450"
    d:DesignWidth="800"
>

    <Grid>

        <DataGrid AutoGenerateColumns="False" Grid.Row="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Process Id" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Process Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Process Executable Path" Binding="{Binding ProcessExecutablePath}"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock x:Name="emptyInstancesNote"
                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                   Text="No activities yet.."
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   Margin="0 175 0 0"
                   IsEnabled="False" />

    </Grid>

</local:ActivityListBase>
