<mahApps:MetroWindow
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:mahApps="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
	xmlns:consoles="clr-namespace:UI.Wpf.Consoles"
	x:Class="UI.Wpf.Shell.ShellView"
	TextElement.FontSize="14"
	TextElement.FontWeight="Medium"
	TextOptions.TextFormattingMode="Ideal"
	TextOptions.TextRenderingMode="ClearType"
	TextElement.Foreground="{DynamicResource MaterialDesignBody}"
	Background="{DynamicResource MaterialDesignPaper}"
	FontFamily="{DynamicResource MaterialDesignFont}"
	ShowTitleBar="False"
	WindowStartupLocation="CenterScreen"
	WindowState="Maximized"
	d:DesignWidth="1280"
	d:DesignHeight="800"
	mc:Ignorable="d"
	Title="dTerm"
	MinWidth="1280"
	MinHeight="800"
	x:Name="shellView"
>

	<Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

		<Path Style="{StaticResource logoVisualStyle}" />

		<Rectangle Fill="Black" HorizontalAlignment="Stretch" Height="1" VerticalAlignment="Bottom" Margin="15 15 15 25" />

		<consoles:ConsoleOptionsPanelView DataContext="{Binding ConsoleOptionsPanelViewModel}" />

		<Button HorizontalAlignment="Right" Margin="25" Style="{StaticResource MaterialDesignFloatingActionButton}" ToolTip="Command Prompt" VerticalAlignment="Bottom">
			<materialDesign:PackIcon Kind="Console" Height="24" Width="24" />
		</Button>

	</Grid>

</mahApps:MetroWindow>
