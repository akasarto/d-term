<mahApps:MetroWindow
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:mahApps="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
	xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:consoles="clr-namespace:UI.Wpf.Consoles"
	xmlns:notebook="clr-namespace:UI.Wpf.Notebook"
	x:Class="UI.Wpf.Shell.ShellView"
	TextElement.FontSize="14"
	TextElement.FontWeight="Medium"
	TextOptions.TextFormattingMode="Ideal"
	TextOptions.TextRenderingMode="ClearType"
	TextElement.Foreground="{DynamicResource MaterialDesignBody}"
	Background="{DynamicResource MaterialDesignPaper}"
	FontFamily="{DynamicResource MaterialDesignFont}"
	ShowTitleBar="False"
	WindowStartupLocation="CenterScreen"
	WindowState="Maximized"
	d:DesignWidth="1280"
	d:DesignHeight="800"
	mc:Ignorable="d"
	Title="dTerm"
	MinWidth="1280"
	MinHeight="800"
	x:Name="shellView"
>

	<Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

		<Path Style="{StaticResource logoVisualStyle}" />

		<Button HorizontalAlignment="Right" Margin="25" Style="{StaticResource MaterialDesignFloatingActionButton}" ToolTip="Command Prompt" VerticalAlignment="Bottom">
			<materialDesign:PackIcon Kind="Console" Height="24" Width="24" />
		</Button>

	</Grid>

	<!--
	<Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="375" />
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>

		<Grid Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<notebook:NotebookWorkspace DataContext="{Binding NotebookWorkspaceViewModel}"
										Grid.Row="0"
										HorizontalAlignment="Stretch"
										VerticalAlignment="Stretch" />

			<materialDesign:Snackbar Grid.Row="1"
									 HorizontalAlignment="Center"
									 MessageQueue="{Binding MessageQueue}"
									 VerticalAlignment="Bottom"
									 x:Name="globalMessages" />

		</Grid>

		<Border
			BorderBrush="{DynamicResource MaterialDesignDivider}"
			BorderThickness="1"
			Grid.Column="1" />

		<consoles:ConsolesWorkspaceView
			DataContext="{Binding ConsolesWorkspaceViewModel}"
			Grid.Column="2"
			HorizontalAlignment="Stretch"
			VerticalAlignment="Stretch" />

	</Grid>
	-->

</mahApps:MetroWindow>
