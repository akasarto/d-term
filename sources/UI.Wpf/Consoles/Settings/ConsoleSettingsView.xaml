<Window
	x:Class="UI.Wpf.Consoles.ConsoleSettingsView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:local="clr-namespace:UI.Wpf.Consoles"
	mc:Ignorable="d"
	Title="ConsoleSettingsView"
	MinWidth="800"
	MinHeight="600"
>

	<Grid>

		<ListView ItemsSource="{Binding Consoles}" SelectionMode="Single">
			<ListView.View>
				<GridView>
					<GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name" />
					<GridViewColumn DisplayMemberBinding="{Binding ProcessPath}" Header="File Path" />
					<GridViewColumn DisplayMemberBinding="{Binding ProcessPathArgs}" Header="File Args" />
					<GridViewColumn DisplayMemberBinding="{Binding ProcessPathType}" Header="Path Type" />
				</GridView>
			</ListView.View>
		</ListView>
		
	</Grid>

</Window>
