<UserControl
	x:Class="UI.Wpf.Consoles.ConsoleOptionView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d" 
	d:DesignHeight="375"
	d:DesignWidth="375"
>

	<Button Command="{Binding CreateInstanceCommand}" CommandParameter="{Binding}" Margin="0 0 15 0" Style="{StaticResource MaterialDesignFloatingActionMiniDarkButton}" ToolTip="{Binding Name}">
		<materialDesign:PackIcon>
			<materialDesign:PackIcon.Template>
				<ControlTemplate>
					<Path Stretch="Uniform" Fill="{TemplateBinding Foreground}" Data="{Binding IconPath}" />
				</ControlTemplate>
			</materialDesign:PackIcon.Template>
		</materialDesign:PackIcon>
	</Button>

</UserControl>
